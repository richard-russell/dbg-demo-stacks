# -----------------------------------------------------------------------------
# Deployment Configuration & Auto-Approval
# 
# Note: Deployments use their default implicit deployment group ({deployment_name}_default).
# -----------------------------------------------------------------------------
